Name : Atharv Suranje

Set A

Q1. select emp_name, salary from employees order by salary desc;

Q2. select emp_name, salary from employees order by salary desc limit 5;

Q3. select * from employees limit 3,5;

Q4. select dept_id, count(*) as "No_of_Employees" from employees group by dept_id;

Q5. select dept_id, avg(salary) from employees group by dept_id having avg(salary)>50000;

Q6. select prod_id, prod_name from Products where prod_name like '%Laptop%';

Q7. select substring(student_name,1,3) as "First_3_Letters" from Students;

Q8. select * from Students where student_name like 'A%';

Q9. select month, count(*) from Sales group by month having count(*) > 100;

Q10. select category, sum(sales), avg(sales) from Sales group by category having sum(sales) > 100000;

Q11. option a

Q12. option b

Q13. option c

Q14. option b

Q15. option b 

Q16. select region, sum(total_amount) as "total_orders" 
     from orders o inner join customers c 
     on o.customer_id = c.customer_id
     group by region 
     having sum(total_amount)>10000;

Q17. select author_id, count(*) as "total_book_sold"
     from Books b inner join Sales s 
     on b.book_id = s.book_id
     group by author_id 
     having count(*) > 100;